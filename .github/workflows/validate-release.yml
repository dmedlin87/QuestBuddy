name: Validate Release Metadata

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate-release:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate addon toolchain contract
        shell: pwsh
        run: |
          ./scripts/Test-AddonProject.ps1
